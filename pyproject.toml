[project]
name = "auto-form"
version = "0.1.0"
description = "Automation helper for community volunteer activity form registration"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.60.0",
    "websockets>=16.0",
]
